# Application runtime dependencies for the One Serious Project API.
# Pinned so local, container, and CI environments resolve the same versions.
fastapi==0.128.8
uvicorn[standard]==0.39.0
sqlalchemy==2.0.52
psycopg[binary]==3.3.5
alembic==1.19.2
